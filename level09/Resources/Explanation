Level09:
Содержит бинарник и непонятные символы.
level09@SnowCrash:~$ ./level09 token
tpmhr
Когда подаём цифры, то видим как они увеличиваются
При подаче 
level09@SnowCrash:~$ ./level09 111111
123456
Соответственн, и аргументы увеличиваются.
Chmod 777 . Чтобы делать что угодно тут.

А так как всё цифры складываются, согласно бинарному, то достаточно написать функцию, чтобы она переводила символы обратно на -1
void		main(void)
{
	char	str[1000];
	int	i = -1;

	scanf("%s", str);
	while(str[++i])
		printf("%c", str[i] - i);
}

Дальше передаём содержание токена на программку и получаем результат
cat token | ./a.out
